[project]
name = "jobfoundry-scorer"
version = "0.1.0"
description = "JobFoundry fit screener + tailoring bridge"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
